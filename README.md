## Introduction

```bash
git submodule add https://github.com/adityatelange/hugo-PaperMod.git themes/hugo-papermod --depth=1 --branch v5.0
git submodule update --init --recursive # needed when you reclone your repo (submodules may not get cloned automatically)
```
